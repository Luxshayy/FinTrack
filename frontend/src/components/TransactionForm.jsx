const EXPENSE_CATS = ['Rent', 'Supplies', 'Payroll', 'Marketing', 'Utilities', 'Software', 'Other'];
const INCOME_CATS = ['Sales', 'Services', 'Other income'];

export default function TransactionForm({ form, editing, saving, onChange, onTypeChange, onSubmit, onCancel }) {
  return <form className="panel" onSubmit={onSubmit}><h2>{editing ? 'Edit entry' : 'New entry'}</h2><div className="form-grid"><label className="field">Date<input required type="date" value={form.date} onChange={(event) => onChange('date', event.target.value)} /></label><label className="field">Type<select value={form.type} onChange={(event) => onTypeChange(event.target.value)}><option value="expense">Expense</option><option value="income">Income</option></select></label></div><label className="field">Description<input required placeholder="e.g. Studio rent" value={form.description} onChange={(event) => onChange('description', event.target.value)} /></label><div className="form-grid"><label className="field">Category<select value={form.category} onChange={(event) => onChange('category', event.target.value)}>{(form.type === 'income' ? INCOME_CATS : EXPENSE_CATS).map((category) => <option key={category}>{category}</option>)}</select></label><label className="field">Amount<input required type="number" min="0.01" step="0.01" value={form.amount} onChange={(event) => onChange('amount', event.target.value)} /></label></div><button className="primary-button" disabled={saving}>{saving ? 'Saving…' : editing ? 'Save changes' : 'Add to ledger'}</button>{editing && <button type="button" className="text-button cancel-button" onClick={onCancel}>Cancel edit</button>}</form>;
}
